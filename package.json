{
  "name": "wip-game",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "classnames": "^2.2.5",
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "react-virtualized": "^9.7.3",
    "rollbar": "^0.6.5",
    "serve": "^5.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "deploy": "now",
    "alias": "now alias wip-game-vuoro.now.sh",
    "now-start": "NODE_ENV=production serve ./build --single --cache 31536000000",
    "now-build": "NODE_ENV=production react-scripts build"
  },
  "devDependencies": {
    "prettier": "^1.3.1",
    "react-scripts": "0.9.5"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  }
}
